// Write a function called sameFrequency. Given two positive integers, find out if the two numberts have the same frequency of digits. Your solution MUST have the following compleities:
// Time: O(N)
// Sample Input:
// sameFrequency(182,281)//true
// sameFrequency(34,14)//false
// sameFrequency(3589578,5879385)//true
// sameFrequency(22,222)//false

function sameFrequency(int1, int2) {
  if (int.length !== int2) {
    false
  }
  for (i = 0; i < int1.length; i++) {
    let j = i + 1
    let array = {}
    array.push(int1[i])
  }
}
